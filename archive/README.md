# Archive

Historical artifacts kept for reference but not needed for day-to-day bot or dashboard operation.

## `reports/`

One-off backtest, sweep, and walk-forward CSV outputs from parameter research (May 2026). New research runs still write to `reports/` at the repo root; those outputs stay local and are gitignored.

## `docs/`

One-time engineering audit snapshots (2026-05-06): architecture overview, codebase/security/performance reviews, tech-debt register, and refactoring roadmap. Ongoing change history lives in [`log.md`](../log.md); active engineering standards live in [`rules/`](../rules/).
